#!/usr/bin/env bash
THEME=$(ls /usr/share/icons | rofi -dmenu -i -p "Cursor Theme")
[[ -z "$THEME" ]] && exit
gsettings set org.gnome.desktop.interface cursor-theme "$THEME"
notify-send "Cursor changed" "$THEME"
